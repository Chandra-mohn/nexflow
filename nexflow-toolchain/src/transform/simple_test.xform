// Minimal Transform - Test Case
// Single primitive input/output, no cache, simple logic

transform double_amount
    version: 1.0.0
    description: "Doubles the input amount"
    pure: true

    input
        amount: decimal
    end

    output
        result: decimal
    end

    apply
        result = amount * 2
    end
end
